<center>

<%= form_for(@negocio) do |f| %>
  <% if @negocio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@negocio.errors.count, "error") %> prohibited this negocio from being saved:</h2>

      <ul>
      <% @negocio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="">
    <%= f.label :produto_id %>
    <%= f.collection_select(:produto_id, Produto.all, :id, :name, prompt: true)  %>
    <%= select("negocio", "tipo", ['Compra', 'Venda'], {include_blank: true}) %>
  </div>
  

  <div class="">
    <%= f.label :quantidade %>
    <%= f.number_field :quantidade %>
  </div>
  <div class="">
    <%= f.label :codigo %>
    <%= f.number_field :codigo %>
  </div>
  <div class="">
    <%= f.label :data %>
    <%= f.date_select :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</center>